import React from 'react';
import { Block, Text } from '../../components';

export default class Browse extends React.Component {
    render() {
        return(
            <Block middle>
                <Text h3 center>Als Student eingeloggt</Text>
            </Block>
        )
    }
}